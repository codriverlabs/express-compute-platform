# AMI builder outputs (manifests, SBOMs, signatures)
ami-builder/output/

# Packer build logs
packer-build-*.log

# CDK build artifacts
ami-builder/cdk/target/
cdk.out/

# Bundle build artifacts
bundle/.build/
