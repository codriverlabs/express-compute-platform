# ecp list-clusters

List all registered clusters (both managed and self-managed).

## Usage

```
ecp list-clusters
```

## Options

None. Uses the configured region from `ecp configure`.

## Examples

```bash
ecp list-clusters
```
